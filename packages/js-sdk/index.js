const { createApplication, deleteApplication, grantx, createKey, updateKey, deleteKey , verifyKey   } = require('./api');

module.exports = {
    createApplication,
    deleteApplication,
    updateKey,
    grantx,
    createKey,
    deleteKey,
    verifyKey
};
